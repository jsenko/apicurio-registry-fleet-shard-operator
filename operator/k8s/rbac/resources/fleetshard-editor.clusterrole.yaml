apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: fleetshard-editor
rules:
  - apiGroups:
      - registry.apicur.io
    resources:
      - fleetshards
    verbs:
      - "*"
  - apiGroups:
      - registry.apicur.io
    resources:
      - fleetshards/status
    verbs:
      - get
